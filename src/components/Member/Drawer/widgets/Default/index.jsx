import DetailMemberDrawer from '@components/Member/Drawer/views';

const DrawerMemberDetailWidget = (props) => {
  return <DetailMemberDrawer {...props} />;
};

export default DrawerMemberDetailWidget;
