// -- component
import VerificationEmailDealerView from '@components/Pages/VerificationEmailDealer/Landing/views';

const VerificationEmailDealerWidget = (props) => {
  return <VerificationEmailDealerView {...props} />;
};

export default VerificationEmailDealerWidget;
