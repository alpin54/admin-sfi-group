// -- component
import DetailOrderView from '@components/Pages/DetailOrder/Landing/views';

const DetailOrderWidget = (props) => {
  return <DetailOrderView {...props} />;
};

export default DetailOrderWidget;
