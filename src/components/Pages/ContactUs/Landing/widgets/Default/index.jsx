// -- component
import ContactUsView from '@components/Pages/ContactUs/Landing/views';

const ContactUsWidget = (props) => {
  return <ContactUsView {...props} />;
};

export default ContactUsWidget;
