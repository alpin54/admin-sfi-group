// -- component
import VerificationEmailMemberView from '@components/Pages/VerificationEmailMember/Landing/views';

const VerificationEmailMemberWidget = (props) => {
  return <VerificationEmailMemberView {...props} />;
};

export default VerificationEmailMemberWidget;
