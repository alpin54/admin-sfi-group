// -- component
import EmailForgotPasswordView from '@components/Pages/EmailForgotPassword/Landing/views';

const EmailForgotPasswordWidget = (props) => {
  return <EmailForgotPasswordView {...props} />;
};

export default EmailForgotPasswordWidget;
