'use client';

import DetailFormSubmissionModal from '@components/FormSubmission/Modal/views';

const MenuModalWidget = (props) => {
  return <DetailFormSubmissionModal {...props} />;
};

export default MenuModalWidget;
